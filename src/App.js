import React from 'react';
import DataDisplay from './DataDisplay'; // Make sure the DataDisplay component is in the same folder or adjust the path accordingly

function App() {
  return (
    <div className="App">
      <DataDisplay />
    </div>
  );
}

export default App;
